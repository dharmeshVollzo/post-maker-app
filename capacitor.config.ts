import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'festival-banner',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
